name: Manual worflow

on:
    workflow_dispatch:
      
jobs:
  demo-first-job:
      runs-on: ubuntu-latest
      steps:
        - run: echo "this is the first job"
  test-tob:
    runs-on: ubuntu-latest
    steps:
      - run: | 
          echo "Hello"
          echo "Worlds"